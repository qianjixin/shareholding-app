version: "3.9"
services:
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    links:
      - "selenium:selenium"
  selenium:
    image: seleniarm/standalone-chromium
    ports:
      - "4444:4444"
